<form method="post" action="{{ path('arrazoia_delete', {'id': arrazoia.id}) }}" onsubmit="return confirm('{{"Seguru elementu hau ezabatu nahi duzula"|trans}}?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arrazoia.id) }}">
    <button class="btn">{{ 'Ezabatu' |trans }}</button>
</form>

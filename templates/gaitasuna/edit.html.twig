{% extends 'base.html.twig' %}

{% block title %}{{"Gaitasuna"|trans}}{% endblock %}

{% block body %}
    <main role="main" class="container">
    
        <div class="row">
            <div class="col">
            <span class="label label-primary label-bs "><h1>"{{"Gaitasuna"|trans}}" {{ "aukera" |trans}} </h1></span>
            </div>
        </div>
        
        <div class="row">
            {{ include('gaitasuna/_form.html.twig') }}
        </div>
        <div class="row">
            <div class="col">
                <a href="{{ path('gaitasuna_index') }}">{{"Atzera"|trans}}</a>
            </div>
            <div class="col">
                {{ include('gaitasuna/_delete_form.html.twig') }}
            </div>
        </div>
    <main>
{% endblock %}

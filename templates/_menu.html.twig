<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path('fitxak_index') }}" ><img src="{{ absolute_url(asset('images/amorebietalogo.png') ) }}"  height="60" alt=""></a>
    
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  {% if is_granted('IS_AUTHENTICATED_FULLY') and '/fitxak' not in app.request.uri  %}
  <a class="navbar-nav" href="{{ path('fitxak_index') }}">{{"Fitxa sozialen zerrenda"|trans}}</a>
  {% endif %}
  
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    
    <ul class="navbar-nav mr-auto">
        {% if is_granted('IS_AUTHENTICATED_FULLY') and is_granted('ROLE_ADMIN') %}
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            {{"Bigarren mailako taulak"|trans}}
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" href="{{ path('genero_index') }}">{{"Genero"|trans}}</a>
                
                <a class="dropdown-item" href="{{ path('nazionalitatea_index') }}">{{"Nazionalitatea"|trans}}</a>
                <a class="dropdown-item" href="{{ path('bizitokia_index') }}">{{"Bizitokia"|trans}}</a>
                <a class="dropdown-item" href="{{ path('bizikidetza_index') }}">{{"Bizikidetza"|trans}}</a>
                
                <a class="dropdown-item" href="{{ path('eragilea_index') }}">{{"Eragilea"|trans}}</a>
                <a class="dropdown-item" href="{{ path('arrazoia_index') }}">{{"Arrazoia"|trans}}</a>
                
                <a class="dropdown-item" href="{{ path('errolda_index') }}">{{"Errolda"|trans}}</a>
                <a class="dropdown-item" href="{{ path('balioespena_index') }}">{{"Balioespena"|trans}}</a>

                <a class="dropdown-item" href="{{ path('gaitasuna_index') }}">{{"Gaitasuna"|trans}}</a>
                <a class="dropdown-item" href="{{ path('jarduerak_index') }}">{{"Jarduerak"|trans}}</a>
                
            </div>
        </li>
        {% endif %}
    </ul>
    
    <ul class="navbar-nav mr-auto">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            {{"Hizkuntza"|trans}}
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
            <a class="dropdown-item" href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge(app.request.query.all|merge({_locale: 'eu'}))) }}">Eu</a>
            <a class="dropdown-item" href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')|merge(app.request.query.all|merge({_locale: 'es'}))) }}">Es</a>
            </div>
        </li>
    <ul>
  </div>
  
  {% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <a class="navbar-nav" href="{{logout_path()}}">{{"Atera" |trans}}</a>
  {% endif %}
   
  
  
</nav>


{% extends 'base.html.twig' %}

{% block title %}Fitxak{% endblock %}

{% block body %}
    <h1>Fitxak</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fitxak.id }}</td>
            </tr>
            <tr>
                <th>Jaiotze_data</th>
                <td>{{ fitxak.jaiotzeData ? fitxak.jaiotzeData|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fitxak_index') }}">back to list</a>

    <a href="{{ path('fitxak_edit', {'id': fitxak.id}) }}">edit</a>

    {{ include('fitxak/_delete_form.html.twig') }}
{% endblock %}

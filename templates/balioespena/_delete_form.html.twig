<form method="post" action="{{ path('balioespena_delete', {'id': balioespena.id}) }}" onsubmit="return confirm('{{"Seguru elementu hau ezabatu nahi duzula"|trans}}?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ balioespena.id) }}">
    <button class="btn">{{ 'Ezabatu' |trans }}</button>
</form>

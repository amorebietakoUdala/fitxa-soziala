{% extends 'base.html.twig' %}

{% block title %}{{ "Jarduerak" |trans}}" {{ "aukerak" |trans}}{% endblock %}

{% block body %}

    

    <main role="main" class="container">
        
        <div class="card">
        
        <div class="card-body">
            <h5 class="card-title">"{{ "Jarduerak" |trans}}" {{ "aukerak" |trans}}</h5>
    
            
            
            {% for jarduerak in jardueraks %}
            <div class="row maila{{jarduerak.lvl}}">
                <div class="col">
                    <span class="label label-default">{{ jarduerak.mailaEu }}</span>
                </div>
                <div class="col">
                    <span class="label label-default">{{ jarduerak.mailaEs }}</span>
                </div>
                <div class="col-1">
                    <span class="label label-default">
                    <a href="{{ path('jarduerak_edit', {'id': jarduerak.id}) }}">{{ "Edit" |trans}}</a>
                    </span>
                </div>
            </div>
            {% else %}
                <span class="label label-default">
                    <td colspan="7">no records found</td>
                </span>
            {% endfor %}
            
            <div class="row">
                <div class="col">
                    <a href="{{ path('jarduerak_new') }}">{{ "berria" |trans}}</a>
                </div>
            </div>
        </div
    </main>

    
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Jarduerak{% endblock %}

{% block body %}
    <h1>Jarduerak</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ jarduerak.id }}</td>
            </tr>
            <tr>
                <th>Maila_eu</th>
                <td>{{ jarduerak.mailaEu }}</td>
            </tr>
            <tr>
                <th>Maila_es</th>
                <td>{{ jarduerak.mailaEs }}</td>
            </tr>
            <tr>
                <th>Lft</th>
                <td>{{ jarduerak.lft }}</td>
            </tr>
            <tr>
                <th>Lvl</th>
                <td>{{ jarduerak.lvl }}</td>
            </tr>
            <tr>
                <th>Rgt</th>
                <td>{{ jarduerak.rgt }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('jarduerak_index') }}">back to list</a>

    <a href="{{ path('jarduerak_edit', {'id': jarduerak.id}) }}">edit</a>

    {{ include('jarduerak/_delete_form.html.twig') }}
{% endblock %}

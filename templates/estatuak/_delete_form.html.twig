<form method="post" action="{{ path('estatuak_delete', {'id': estatuak.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ estatuak.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Herria index{% endblock %}

{% block body %}
    <h1>Herria index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Herria</th>
                <th>Kodea</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for herrium in herrias %}
            <tr>
                <td>{{ herrium.id }}</td>
                <td>{{ herrium.herria }}</td>
                <td>{{ herrium.kodea }}</td>
                <td>
                    <a href="{{ path('herria_show', {'id': herrium.id}) }}">show</a>
                    <a href="{{ path('herria_edit', {'id': herrium.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('herria_new') }}">Create new</a>
{% endblock %}

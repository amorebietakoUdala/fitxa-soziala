<form method="post" action="{{ path('herria_delete', {'id': herrium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ herrium.id) }}">
    <button class="btn">Delete</button>
</form>

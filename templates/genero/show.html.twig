{% extends 'base.html.twig' %}

{% block title %}Genero{% endblock %}

{% block body %}
    <h1>Genero</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genero.id }}</td>
            </tr>
            <tr>
                <th>Genero_eu</th>
                <td>{{ genero.generoEu }}</td>
            </tr>
            <tr>
                <th>Genero_es</th>
                <td>{{ genero.generoEs }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('genero_index') }}">back to list</a>

    <a href="{{ path('genero_edit', {'id': genero.id}) }}">edit</a>

    {{ include('genero/_delete_form.html.twig') }}
{% endblock %}

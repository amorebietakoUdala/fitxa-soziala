{% extends 'base.html.twig' %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block title %}{{ "Nazionalitatea" |trans}} ...{{ "berria" |trans}} {% endblock %}

{% block body %}
    <main role="main" class="container">
        <h1>"{{ "Nazionalitatea" |trans}}" {{ "berria" |trans}} </h1>

        {{ include('nazionalitatea/_form.html.twig') }}

        <a href="{{ path('nazionalitatea_index') }}">{{ "Atzera" |trans}}</a>
    </main>
{% endblock %}
